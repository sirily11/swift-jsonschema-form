import SwiftUI

// Placeholder view for CopyButton
struct CopyButton: View {
    var body: some View {
        Button { /* Action */ } label: { Image(systemName: "doc.on.doc") }
    }
}

// Placeholder view for MoveDownButton
struct MoveDownButton: View {
    var body: some View {
        Button { /* Action */ } label: { Image(systemName: "arrow.down") }
    }
}

// Placeholder view for MoveUpButton
struct MoveUpButton: View {
    var body: some View {
        Button { /* Action */ } label: { Image(systemName: "arrow.up") }
    }
}

// Placeholder view for RemoveButton
struct RemoveButton: View {
    var body: some View {
        Button { /* Action */ } label: { Image(systemName: "trash") }
    }
} 